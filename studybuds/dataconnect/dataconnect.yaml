specVersion: "v1"
serviceId: "studybuds"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "studybuds-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
